# Python Backend Requirements for YoopRL Trading System

# Web Framework
flask==3.0.0
flask-cors==4.0.0

# Database (SQLite is built into Python, but we need these for better functionality)
# None needed - SQLite is included in Python standard library

# Data Processing
numpy>=1.26.0
pandas>=2.1.0
# scipy>=1.11.0  # Removed - using pure NumPy for drift detection (PSI)

# Data Download & Technical Analysis
yfinance>=0.2.28
ta>=0.10.2
textblob>=0.17.0
aiohttp>=3.8.0

# Reinforcement Learning - Updated for Training Module
stable-baselines3==2.1.0
gymnasium==0.29.1
shimmy==0.2.1  # Compatibility helpers for Gymnasium environments
optuna>=3.0.0  # Hyperparameter optimization
tensorboard>=2.20.0  # Stable-Baselines3 logging backend

# PyTorch with CUDA support (for GPU acceleration)
# Install manually: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# For CPU only: pip install torch torchvision torchaudio
# torch>=2.0.0 (installed via stable-baselines3)

# Task Scheduling (for Retraining)
schedule>=1.2.0

# IBKR Integration (Python adapter)
PyQt6==6.7.0
requests==2.31.0

# Logging and Monitoring
colorlog==6.7.0

# Environment Variables
python-dotenv>=1.0.0

# Testing
pytest==7.4.3
pytest-cov==4.1.0
